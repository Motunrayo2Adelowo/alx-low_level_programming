#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: counts arguments
 * @argv: arguments
 * Return: on success 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
